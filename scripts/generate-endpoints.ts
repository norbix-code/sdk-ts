// This file is intentionally a stub. The active codegen lives in
// `generate-endpoints.mjs` so it can run with plain Node (no tsx, no extra
// devDeps). Edit that file instead.
//
// Kept here only so editors don't lose import paths if the .ts version
// shows up in old branches.
export {};
